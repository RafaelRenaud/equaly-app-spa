import { RncStatusPipe } from './rnc-status.pipe';

describe('RncStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RncStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
